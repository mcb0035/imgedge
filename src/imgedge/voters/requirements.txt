# Optional second voter: Hugging Face / timm ImageNet model (MobileNetV3-Large).
# Install these to enable the timm voter. The server runs fine without them --
# the voter is detected at load time and skipped if torch/timm are missing.
#
#   pip install -r voters/requirements.txt
#
# torch is large; pick the build that matches your hardware from
# https://pytorch.org/get-started/locally/ if you want CUDA/XPU acceleration.
#
# Supply chain: pin exact versions and install with --require-hashes for a
# verified, reproducible env (see src/imgedge/inat/requirements.txt for the recipe).
timm==1.0.27
torch==2.13.0
numpy==2.5.1
pillow==12.3.0
