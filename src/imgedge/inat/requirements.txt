# Supply chain: for a tamper-proof, reproducible install, pin versions AND
# hashes and install with --require-hashes (pip then rejects any wheel whose
# hash doesn't match). Generate a lock from this file with, e.g.:
#   pip install pip-tools
#   pip-compile --generate-hashes -o src/imgedge/inat/requirements.lock src/imgedge/inat/requirements.txt
#   pip install --require-hashes -r src/imgedge/inat/requirements.lock
numpy==2.5.1
pillow==12.3.0
ai-edge-litert==2.1.6            # native LiteRT runtime (the server prefers this)

# Heavier alternative runtime — bundles tf.lite.Interpreter; use instead of
# ai-edge-litert only if LiteRT has no wheel for your platform:
#   pip install tensorflow

# Optional GPU/NPU acceleration (convert first: python convert_to_onnx.py):
#   pip install tf2onnx onnx           # TFLite -> ONNX conversion (needs tensorflow)
#   pip install onnxruntime-openvino   # Intel NPU / GPU  (server default: NPU)
#   pip install onnxruntime-directml   # any DX12 GPU (NVIDIA / Intel / AMD)
#   pip install onnxruntime-gpu        # NVIDIA CUDA
