# Fine-tuning deps. Install CPU build with:  pip install -r requirements.txt
# For NVIDIA CUDA, install torch/torchvision from the official index first, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Fine-tuning deps. Install CPU build with:  pip install -r requirements.txt
# For NVIDIA CUDA, install torch/torchvision from the official index first, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# Versions are pinned (==) for reproducibility and so OSV/Scorecard doesn't flag
# the old floor versions a `>=` range would otherwise resolve to. Bump together
# when refreshing (these match the shipped voter/core pins).
torch==2.13.0
torchvision==0.28.0
onnx==1.22.0
onnxruntime==1.28.0
pillow==12.3.0
numpy==2.5.1
